desc: Simple example for testing only [updated]


parts:
  cube:
    desc: This is a cube from examples
    type: cadquery
    parameters:
      width:
        default: 10.0
      length:
        default: 10.0
      height: 10.0
